import React from 'react'

export default function Header({title}) {
    return (
        <header>
            <h1>{title}</h1>

            {/* Children: all the content that our class has received */}


        </header>
    )

}